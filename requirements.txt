# White Tree Nexus — backend (wtnapp) dependencies
fastapi
uvicorn[standard]
sqlalchemy>=2.0
pydantic>=2
email-validator
alembic
python-jose[cryptography]
argon2-cffi
slowapi
redis
python-dotenv
psycopg[binary]        # PostgreSQL driver (produção)

# --- dev / test ---
pytest
httpx                  # FastAPI TestClient
